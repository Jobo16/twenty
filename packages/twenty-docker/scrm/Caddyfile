{
  email {$SCRM_TLS_EMAIL}

  log {
    output stdout
    format json
  }
}

{$SCRM_PUBLIC_HOST} {
  encode zstd gzip

  header {
    -Server
  }

  reverse_proxy server:3000 {
    health_uri /healthz
    health_interval 10s
    health_timeout 5s
  }
}
